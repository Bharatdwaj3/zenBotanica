events {}
http {
  server {
    listen 3100;
    auth_basic "Loki";
    auth_basic_user_file /etc/nginx/.htpasswd;

    location / {
      proxy_pass http://localhost:3101;
      proxy_set_header Host $host;
    }
  }
}
