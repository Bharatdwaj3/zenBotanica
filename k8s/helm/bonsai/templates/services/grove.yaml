apiVersion: v1
kind: Service
metadata:
  name: grove
  labels:
    bonsai-service: grove
  {{- include "bonsai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.grove.type }}
  selector:
    bonsai-service: grove
    {{- include "bonsai.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.grove.ports | toYaml | nindent 2 }}
---
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: grove
  labels:
    bonsai-service: grove
  {{- include "bonsai.labels" . | nindent 4 }}
spec:
  progressDeadlineSeconds: 120
  replicas: 3
  selector:
    matchLabels:
      bonsai-service: grove
  strategy:
    canary:
      steps:
      - setWeight: 20
      - pause:
          duration: 30s
      - setWeight: 40
      - pause:
          duration: 30s
      - setWeight: 60
      - pause:
          duration: 30s
      - setWeight: 80
      - pause:
          duration: 30s
      - setWeight: 100
  template:
    metadata:
      labels:
        bonsai-service: grove
    spec:
      containers:
      - args:
        - sh
        - -c
        - npx prisma generate && npx prisma migrate deploy && npm start
        envFrom:
        - secretRef:
            name: grove-secrets
        image: ghcr.io/bharatdwaj3/botany-grove:08e7964
        name: grove-ctr
        ports:
        - containerPort: 4001
        readinessProbe:
          failureThreshold: 3
          initialDelaySeconds: 5
          periodSeconds: 5
          tcpSocket:
            port: 4001
        resources: {}
      imagePullSecrets:
      - name: ghcr-secret
      restartPolicy: Always
status: {}
