apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: visitor-services-data
  labels:
    io.kompose.service: visitor-services-data
  {{- include "bonsai.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.data.storageRequest | quote }}
