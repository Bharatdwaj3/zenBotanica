apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grove-data
  labels:
    io.kompose.service: grove-data
  {{- include "bonsai.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.groveData.storageRequest | quote }}
