apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gardeners-data
  labels:
    io.kompose.service: gardeners-data
  {{- include "bonsai.labels" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.gardenersData.storageRequest | quote }}
