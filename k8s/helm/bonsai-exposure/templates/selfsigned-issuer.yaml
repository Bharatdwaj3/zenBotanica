apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: {{ include "bonsai-exposure.fullname" . }}-selfsigned-issuer
  labels:
  {{- include "bonsai-exposure.labels" . | nindent 4 }}
spec:
  selfSigned: {}
