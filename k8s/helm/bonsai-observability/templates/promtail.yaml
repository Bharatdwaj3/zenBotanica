apiVersion: v1
kind: Service
metadata:
  name: {{ include "bonsai-observability.fullname" . }}-promtail
  labels:
    io.kompose.service: promtail
  {{- include "bonsai-observability.labels" . | nindent 4 }}
  annotations:
    kompose.cmd: kompose convert -f observability-compose.yml -o k8s/observability/
    kompose.version: 1.31.2 (a92241f79)
spec:
  type: {{ .Values.promtail.type }}
  selector:
    io.kompose.service: promtail
    {{- include "bonsai-observability.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.promtail.ports | toYaml | nindent 2 }}
