apiVersion: v1
kind: Service
metadata:
  name: {{ include "bonsai-services.fullname" . }}-gardeners
  labels:
    bonsai-service: gardeners
  {{- include "bonsai-services.labels" . | nindent 4 }}
  annotations:
    kompose.cmd: kompose convert -f docker-compose.yml --out k8s/services/
    kompose.version: 1.31.2 (a92241f79)
spec:
  type: {{ .Values.gardeners.type }}
  selector:
    bonsai-service: gardeners
    {{- include "bonsai-services.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.gardeners.ports | toYaml | nindent 2 }}
---
apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: {{ include "bonsai-services.fullname" . }}-gardeners
  labels:
    bonsai-service: gardeners
  {{- include "bonsai-services.labels" . | nindent 4 }}
spec:
  progressDeadlineSeconds: 120
  replicas: 3
  selector:
    matchLabels:
      bonsai-service: gardeners
  strategy:
    canary:
      steps:
      - setWeight: 20
      - pause:
          duration: 30s
      - setWeight: 40
      - pause:
          duration: 30s
      - setWeight: 60
      - pause:
          duration: 30s
      - setWeight: 80
      - pause:
          duration: 30s
      - setWeight: 100
  template:
    metadata:
      labels:
        bonsai-service: gardeners
    spec:
      containers:
      - args:
        - sh
        - -c
        - npx prisma generate && npx prisma migrate deploy && npm start
        envFrom:
        - secretRef:
            name: gardeners-secrets
        image: {{ .Values.gardeners.image.repository }}:{{ .Values.gardeners.image.tag | default .Chart.AppVersion }}
        name: gardeners-ctr
        ports:
        - containerPort: 4003
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          initialDelaySeconds: 5
          periodSeconds: 5
          tcpSocket:
            port: 4003
        resources: {{- toYaml .Values.gardeners.resources | nindent 10 }}
      imagePullSecrets:
      - name: ghcr-secret
      restartPolicy: Always
status: {}
