apiVersion: v1
kind: Service
metadata:
  name: {{ include "bonsai-services.fullname" . }}-grove-db
  labels:
    app: grove-db
  {{- include "bonsai-services.labels" . | nindent 4 }}
spec:
  type: {{ .Values.groveDb.type }}
  selector:
    app: grove-db
    {{- include "bonsai-services.selectorLabels" . | nindent 4 }}
  ports:
  {{- .Values.groveDb.ports | toYaml | nindent 2 }}
